node_modules/
dist/
coverage/
*.tgz

# Security audit run outputs (corpus, model report, SARIF) — never committed
.security-audit/

# Sample app build outputs (the sample SOURCES under samples/ are committed)
samples/**/bin/
samples/**/obj/
samples/**/.publish/
